import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h2>This is Inventory page and change with git own repo</h2>
        </div>
    );
};

export default Inventory;